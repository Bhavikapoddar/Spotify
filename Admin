/* Global Styles for Body */
body {
    /* A softer, more inviting blue gradient for the background */
    background: linear-gradient(to right, #e6f2ff, #f0f8ff);
    font-family: 'Segoe UI', 'Helvetica Neue', Arial, sans-serif; /* Slightly updated font stack */
    color: #333; /* Darker, more readable text color */
    margin: 0;
    padding: 30px; /* Increased padding for more breathing room */
    background-attachment: fixed;
    background-size: cover;
    line-height: 1.6; /* Improved line spacing for readability */
}

/* Main Container */
.passenger-container {
    max-width: 900px; /* Slightly narrower for a more focused layout */
    margin: 0 auto;
    padding: 40px; /* More internal padding */
    background: rgba(255, 255, 255, 0.98); /* Near-opaque white for a clean base, no blur */
    border-radius: 25px; /* Softer, more modern rounded corners */
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15); /* More pronounced, softer shadow */
}

/* Card Styling */
.passenger-card {
    background-color: #ffffff; /* Pure white for crispness */
    border-radius: 18px; /* Slightly larger radius for cards */
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08); /* Gentle shadow for depth */
    padding: 40px; /* Increased padding inside cards */
    margin-bottom: 35px; /* More space between cards */
    transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out; /* Smooth transitions */
    border: 1px solid #e0e0e0; /* Subtle border for definition */
}

.passenger-card:hover {
    transform: translateY(-5px); /* Slight lift effect on hover */
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15); /* Enhanced shadow on hover */
}

/* Heading Styles */
h2.passenger-title {
    font-weight: 700; /* Slightly less bold, but still strong */
    color: #2c3e50; /* Deep, rich blue-grey for main title */
    text-align: center;
    margin-bottom: 4rem; /* More space below the main title */
    font-size: 2.5rem; /* Larger, more impactful main title */
    letter-spacing: 0.5px; /* Reduced letter spacing for better readability */
    position: relative;
    padding-bottom: 15px; /* Space for an underline effect */
}

h2.passenger-title::before {
    content: "✈️ ";
    font-size: 2rem; /* Larger emoji */
    margin-right: 10px;
}

h2.passenger-title::after {
    content: '';
    position: absolute;
    left: 50%;
    bottom: 0;
    transform: translateX(-50%);
    width: 80px; /* Short underline */
    height: 4px;
    background: linear-gradient(to right, #4A90E2, #357ABD); /* Gradient underline */
    border-radius: 2px;
}

.passenger-card h5 {
    font-weight: 600; /* Slightly less bold for sub-headings */
    color: #4A90E2; /* Vibrant blue for card titles */
    font-size: 1.35rem; /* Slightly larger card titles */
    margin-bottom: 25px; /* More space below card titles */
    border-bottom: 1px solid #f0f0f0; /* Subtle separator */
    padding-bottom: 10px;
}

.passenger-card h5::before {
    content: "📝 ";
    margin-right: 8px;
}

/* Column Layout (Responsive using floats) */
.row::after {
    content: "";
    display: table;
    clear: both;
}

.col-md-3, .col-md-4, .col-md-6 {
    float: left;
    padding: 0 15px;
    box-sizing: border-box;
    margin-bottom: 25px; /* More vertical spacing between fields */
}

.col-md-3 { width: 25%; }
.col-md-4 { width: 33.3333%; }
.col-md-6 { width: 50%; }

/* Responsive for Mobile (Media Query) */
@media (max-width: 768px) {
    body {
        padding: 15px; /* Less padding on very small screens */
    }
    .passenger-container {
        padding: 25px; /* Less padding inside container on small screens */
        border-radius: 15px; /* Smaller border-radius on small screens */
    }
    .passenger-card {
        padding: 25px; /* Less padding inside cards on small screens */
        border-radius: 12px; /* Smaller border-radius for cards on small screens */
    }
    h2.passenger-title {
        font-size: 2rem; /* Adjust title size for smaller screens */
        margin-bottom: 3rem;
    }
    .passenger-card h5 {
        font-size: 1.2rem; /* Adjust card title size */
    }
    .col-md-3, .col-md-4, .col-md-6 {
        width: 100%; /* Columns stack vertically */
        float: none; /* Remove float */
        padding: 0;
        margin-bottom: 20px; /* Consistent spacing when stacked */
    }
}

/* Labels and Inputs */
.passenger-label {
    display: block;
    font-weight: 600;
    color: #555; /* Softer dark grey for labels */
    margin-bottom: 10px; /* More space below labels */
    font-size: 0.95rem; /* Slightly smaller label font */
}

.passenger-input, .passenger-select {
    width: 100%;
    padding: 15px 18px; /* Increased padding for larger input fields */
    border: 1px solid #dcdcdc; /* Lighter, softer border */
    border-radius: 12px; /* More rounded input fields */
    background-color: #fcfcfc; /* Very light background for inputs */
    color: #333;
    transition: all 0.3s ease-in-out;
    font-size: 1.05rem; /* Slightly larger input text */
    box-shadow: inset 0 1px 4px rgba(0, 0, 0, 0.04); /* Subtle inner shadow */
}

.passenger-input:focus, .passenger-select:focus {
    border-color: #4A90E2; /* Vibrant blue border on focus */
    box-shadow: 0 0 0 4px rgba(74, 144, 226, 0.25); /* Soft blue glow on focus */
    background-color: #fff;
    outline: none;
}

.passenger-input[readonly], .passenger-select[readonly] {
    background-color: #f0f0f0; /* Lighter grey for readonly fields */
    cursor: not-allowed;
    box-shadow: none; /* No inner shadow for readonly */
}

/* Buttons */
.button-group {
    text-align: right;
    margin-top: 40px; /* More space above buttons */
}

.passenger-btn-primary {
    /* More vibrant blue gradient for primary button */
    background: linear-gradient(to right, #4A90E2, #357ABD);
    color: white;
    border: none;
    padding: 16px 35px; /* Larger buttons */
    font-size: 1.1rem; /* Larger font size for buttons */
    font-weight: 700;
    border-radius: 12px; /* Rounded button corners */
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1); /* Subtle button shadow */
}

.passenger-btn-primary:hover {
    background: linear-gradient(to right, #357ABD, #2a5f96); /* Darker gradient on hover */
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15); /* Enhanced shadow on hover */
    transform: translateY(-2px); /* Slight lift on hover */
}

.passenger-btn-secondary {
    background: #a0aec0; /* Muted grey for secondary button */
    color: white;
    border: none;
    padding: 16px 35px; /* Larger buttons */
    font-size: 1.1rem; /* Larger font size for buttons */
    font-weight: 700;
    border-radius: 12px; /* Rounded button corners */
    cursor: pointer;
    transition: all 0.3s ease;
    margin-right: 15px; /* More space between buttons */
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.08); /* Subtle button shadow */
}

.passenger-btn-secondary:hover {
    background-color: #718096; /* Darker grey on hover */
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.12); /* Enhanced shadow on hover */
    transform: translateY(-2px); /* Slight lift on hover */
}

/* Spacing */
.passenger-mb-5 {
    margin-bottom: 5rem !important;
}
