@{
    ViewBag.Title = "Admin Dashboard";
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #eaf0f6;
            margin: 0;
            padding: 0;
        }

        .header {
            background-color: #A6CAF0;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: #f0f8ff; /* Light text color */
            font-size: 18px;
            font-weight: bold;
        }

        .logout-link {
            font-weight: bold;
            color: red;
            text-decoration: none;
            margin-left: auto;
        }

        .container {
            display: flex;
            height: calc(100vh - 70px);
        }

        .sidebar {
            background-color: #004080;
            width: 200px;
            padding: 20px;
            color: white;
        }

        .sidebar a {
            display: block;
            margin: 10px 0;
            color: white;
            text-decoration: none;
            background-color: #6699cc;
            padding: 10px;
            border-radius: 5px;
        }

        .sidebar a:hover {
            background-color: #336699;
        }

        .main {
            flex: 1;
            padding: 30px;
            background-color: #f5faff;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .main img {
            max-width: 300px;
            height: auto;
            margin-bottom: 20px;
        }

        .main h2 {
            color: #005580;
            font-size: 24px;
        }
    </style>
</head>
<body>
    <div class="header">
        <span>Welcome Admin @Session["username"]</span>
        <a class="logout-link" href="@Url.Action("Logout", "Login")">Logout</a>
    </div>

    <div class="container">
        <div class="sidebar">
            <a href="@Url.Action("Index", "Customer")">Customer Master</a>
            <a href="@Url.Action("Index", "Room")">Room Master</a>
            <a href="@Url.Action("Index", "Staff")">Staff Info</a>
            <a href="@Url.Action("Index", "RoomStatus")">Room Status</a>
        </div>

        <div class="main">
            <img src="~/Content/Images/hotel.png" alt="Hotel Image" />
            <h2>Welcome to VITS</h2>
        </div>
    </div>
</body>
</html>
