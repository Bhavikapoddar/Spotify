using System.Web;
using System.Web.Mvc;

namespace Airline_Managementnew.Filters
{
    public class AuthFilter : ActionFilterAttribute
    {
        public override void OnActionExecuting(ActionExecutingContext filterContext)
        {
            var session = filterContext.HttpContext.Session;

            // Controller and Action Names
            string controller = filterContext.ActionDescriptor.ControllerDescriptor.ControllerName;
            string action = filterContext.ActionDescriptor.ActionName;

            // Allow these actions without login
            if (controller == "User" && (action == "Login" || action == "Register"))
                return;

            // Redirect if not logged in
            if (session["AdminId"] == null && session["UserId"] == null)
            {
                // Write debug log
                System.Diagnostics.Debug.WriteLine($"Blocked: {controller}/{action}");

                // Redirect to login
                filterContext.Result = new RedirectResult("~/User/Login");
            }

            base.OnActionExecuting(filterContext);
        }
    }
}
